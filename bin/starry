#!/usr/bin/env bash
# Starry Code launcher (POSIX). Forwards all args to starry.py.
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]:-$0}")" >/dev/null 2>&1 && pwd)"
exec python3 "${SCRIPT_DIR}/starry.py" "$@"
